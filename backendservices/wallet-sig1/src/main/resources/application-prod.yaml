sw:
  wallet:
    network: main
    # Production master/private keys must be injected by KMS/HSM or environment variables.
    masterKey: ${SW_SIG1_MASTER_KEY:}
    pubKey1: ${SW_PROD_PUBKEY1:}
    pubKey2: ${SW_PROD_PUBKEY2:}
    pubKey3: ${SW_PROD_PUBKEY3:}

