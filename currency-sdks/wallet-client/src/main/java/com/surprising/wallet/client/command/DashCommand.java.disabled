package com.surprising.wallet.client.command;

import com.surprising.wallet.common.annotation.RpcConfig;

/**
 * @author lilaizhen
 * @data 09/04/2018
 */
@RpcConfig(
        server = "${atomex.dash.server}",
        username = "${atomex.dash.server.user}",
        password = "${atomex.dash.server.pwd}"
)
public interface DashCommand extends BtcLikeCommand {
}
